import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <footer>
      <div class="container" style={{ textAlign: "center", padding: "2rem", color: "#6b7280", fontSize: "0.875rem" }}>
        <span>Living Arts - AI Video Production</span>
      </div>
    </footer>
  );
});
